version: 2
jobs:
  build:
    working_directory: ~/front
    docker:
      - image: circleci/node:8-browsers
    steps:
      - run: pwd
      - run: npm install
      - run: npm run build:prod
workflows:
  version: 2
  build:
    jobs:
      - build
